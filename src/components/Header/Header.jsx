import React from 'react';

const Header = () => {
    return (
        <header>
            <a href="">Home Page</a>
            <a href="">About Page</a>
        </header>
    );
};

export default Header;